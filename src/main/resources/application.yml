server:
  port: 8000
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
    
spring:

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
      
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/allp?serverTimezone=Asia/Seoul
    username: bm
    password: bm1234
    
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false # jpa가 사용하는 기본 넘버링을 따라가지 않는다는 의미
    show-sql: false
    properties:
      hibernate.format_sql: true

  jackson:
    serialization:
      fail-on-empty-beans: false
      
  mail:
    host: smtp.gmail.com
    port: 587
    username: ikerbm94@gmail.com
    password: rlaqhals1!
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true